{
  "name": "zontes150x-netlify",
  "private": true,
  "version": "2.0.0",
  "description": "ZONTES 150X Maintenance Log — self-contained on Netlify (Netlify Functions + Netlify Blobs, no Google Sheets / Apps Script)",
  "dependencies": {
    "@netlify/blobs": "^10.0.0"
  }
}
